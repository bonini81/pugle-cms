import "./Header.scss";
import PcLinks from "./PcLinks";

const HeaderMenu = () => {
  return (
    <nav className="orientation-menu-desktop">
      <PcLinks />
    </nav>
  );
};

export default HeaderMenu;
